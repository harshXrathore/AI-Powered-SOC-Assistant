# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Data / validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]==2.9.2
email-validator==2.2.0

# --- Database ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.2

# --- Cache / queue ---
redis==5.0.8
celery[redis]==5.4.0

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
# Pinned below 4.1: passlib 1.7.4's bcrypt backend self-test breaks on
# bcrypt>=4.1 (AttributeError on bcrypt.__about__ / ValueError on its
# internal 72-byte wrap-bug probe). Known passlib/bcrypt compatibility
# issue, not a project bug — pin until passlib releases a fix.
bcrypt==4.0.1

# --- HTTP clients ---
httpx==0.27.2
aiohttp==3.10.5

# --- AI / LLM ---
anthropic==0.34.2
langchain==0.3.1
langchain-anthropic==0.2.1

# --- MITRE ATT&CK ---
mitreattack-python==3.0.6

# --- Realtime ---
python-socketio==5.11.4

# --- PDF / report generation ---
weasyprint==62.3
markdown==3.7

# --- Utilities ---
python-dotenv==1.0.1
tenacity==9.0.0
structlog==24.4.0
python-dateutil==2.9.0.post0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx[http2]==0.27.2
respx==0.21.1
aiosqlite==0.20.0
fakeredis==2.24.1
